# Worker lightweight requirements — for local dev (no torch/GPU).
# The diarization, transcription, and sentiment tasks fall back to stubs
# when these heavy packages are absent, so the entire pipeline is testable.

celery==5.4.0
redis[hiredis]==5.0.4
sqlalchemy[asyncio]==2.0.29
asyncpg==0.29.0
pydantic-settings==2.2.1
structlog==24.1.0
httpx==0.27.0

# Phase 2 — RAG (lightweight; no torch needed)
openai==1.30.1
anthropic==0.26.0
qdrant-client==1.9.1
tiktoken==0.7.0
pypdf==4.2.0
python-docx==1.1.2
langchain-text-splitters==0.2.0

# Dev / test
pytest==8.2.0
pytest-asyncio==0.23.6
